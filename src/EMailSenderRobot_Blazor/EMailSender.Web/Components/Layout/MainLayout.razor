@using EMailSender.Core.Services
@using EMailSender.Core.Models
@using System.Reflection
@inherits LayoutComponentBase

<div class="d-flex flex-column" style="min-height:100vh">
    <nav class="navbar navbar-dark bg-dark px-3">
        <a class="navbar-brand fw-bold me-4" href="/">EMailSender</a>
        <ul class="nav me-3">
            <li class="nav-item">
                <NavLink class="nav-link text-secondary px-3" href="/" Match="NavLinkMatch.All">
                    Monitor mail
                </NavLink>
            </li>
            <li class="nav-item">
                <NavLink class="nav-link text-secondary px-3" href="/log">
                    Scheduler Logs
                </NavLink>
            </li>
            <li class="nav-item">
                <NavLink class="nav-link text-secondary px-3" href="/emaileditor">
                    Email Editor
                </NavLink>
            </li>
            <li class="nav-item">
                <NavLink class="nav-link text-secondary px-3" href="/companies">
                    Impostazioni Tenant
                </NavLink>
            </li>
            <li class="nav-item">
                <NavLink class="nav-link text-secondary px-3" href="/smtpconfig">
                    Config SMTP
                </NavLink>
            </li>
        </ul>
        <span class="text-secondary small">v@_version</span>
    </nav>

    @* Banner warning configurazione *@
    @if (_warnings.Any())
    {
        <div class="alert alert-warning mb-0 rounded-0 py-2 px-4" style="border-left: 4px solid #fd7e14;">
            <strong>Attenzione — configurazione incompleta:</strong>
            <ul class="mb-0 mt-1">
                @foreach (var w in _warnings)
                {
                    <li>@w</li>
                }
            </ul>
        </div>
    }

    <main class="container-fluid py-4 px-4">
        @Body
    </main>
</div>

@code {
    [Inject] ConfigService Cfg { get; set; } = default!;
    private string _version = "";
    private List<string> _warnings = [];

    protected override void OnInitialized()
    {
        _version = Assembly.GetExecutingAssembly()
                           .GetName()
                           .Version?
                           .ToString() ?? "—";
        _warnings = Cfg.GetConfigWarnings();
    }
}